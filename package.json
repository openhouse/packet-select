{
  "name": "packet-select",
  "version": "0.1.0",
  "description": "Curate project trees into frequency-based packets via simulated LLM curator meetings.",
  "type": "module",
  "bin": {
    "packet-select": "bin/packet-select.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "openai": "^4.73.0"
  }
}
